apiVersion: v1
kind: Pod
metadata:
  name: aws-cli
  namespace: default
spec:
  serviceAccountName: aws-cli-sa-j
  containers:
  - name: aws-cli
    image: amazon/aws-cli
    command: ["sleep", "infinity"]